# 🎂 คำแนะนำการตั้งค่าเว็บไซต์วันเกิด

## 📸 ขั้นตอนที่ 1: เพิ่มรูปภาพ

1. ไปที่โฟลเดอร์ `public/photos/`
2. เพิ่มรูปภาพ 10 รูป ตั้งชื่อไฟล์เป็น:
   - `1.jpg`
   - `2.jpg`
   - `3.jpg`
   - `4.jpg`
   - `5.jpg`
   - `6.jpg`
   - `7.jpg`
   - `8.jpg`
   - `9.jpg`
   - `10.jpg`

**หมายเหตุ:** รองรับไฟล์ `.jpg`, `.jpeg`, `.png`

---

## 🎵 ขั้นตอนที่ 2: เพิ่มเพลง

1. ไปที่โฟลเดอร์ `public/music/`
2. เพิ่มไฟล์เพลง ตั้งชื่อเป็น `birthday-song.mp3`

**หมายเหตุ:** รองรับไฟล์ `.mp3`, `.wav`, `.ogg`

---

## ✍️ ขั้นตอนที่ 3: แก้ไขข้อความ

1. เปิดไฟล์ `app/components/PhotoGallery.tsx`
2. หาส่วน `const photos: PhotoData[]` (บรรทัดที่ 12)
3. แก้ไขข้อความในแต่ละรูป:

```typescript
const photos: PhotoData[] = [
  {
    id: 1,
    imagePath: "/photos/1.jpg",
    message: "ข้อความที่คุณต้องการ ❤️", // <-- แก้ไขที่นี่
  },
  // ... ต่อไปอีก 9 รูป
]
```

---

## 🚀 ขั้นตอนที่ 4: รันเว็บไซต์

### Development Mode (สำหรับทดสอบ):
```bash
npm run dev
```
เปิด browser ไปที่ http://localhost:3000

### Production Mode (สำหรับใช้งานจริง):
```bash
npm run build
npm start
```

---

## ✨ ฟีเจอร์ของเว็บไซต์

### 📍 User Flow:
1. **เปิดเว็บ** → เห็นหน้า "Happy Birthday" พร้อมตัวเลข 27
2. **กดปุ่ม "Start Countdown"** → เริ่มนับถอยหลัง 27 วินาที พร้อม circular progress bar
3. **นับเสร็จ** → เปลี่ยนไปหน้า Photo Gallery อัตโนมัติ

### 🎵 ในหน้า Photo Gallery:
1. **Music Player** - อยู่ด้านบนสุด กดปุ่มเพื่อเปิด/ปิดเพลง (loop ต่อเนื่อง)
2. **ข้อความ** - แสดงด้านบนรูปภาพ (นอก container)
3. **รูปภาพ** - 10 รูป แสดงทีละรูป
   - กดปุ่ม Previous/Next เพื่อเปลี่ยนรูป
   - กด Arrow keys (← →) บน keyboard ก็ได้
   - คลิก dots indicator เพื่อข้ามไปรูปที่ต้องการ
4. **Pink Theme** - ธีมสีชมพู romantic ทั้งหมด
5. **Responsive** - ใช้งานได้ทั้ง mobile, tablet, desktop

---

## 🎨 การปรับแต่งเพิ่มเติม

### เปลี่ยนสีธีม:
แก้ไขในไฟล์ `app/globals.css` ที่ส่วน `:root`

### เปลี่ยนเวลา Countdown:
แก้ไขในไฟล์ `app/components/Countdown.tsx` บรรทัดที่ 7:
```typescript
const [timeLeft, setTimeLeft] = useState(27) // เปลี่ยนจาก 27 เป็นเวลาที่ต้องการ
```

### เปลี่ยนจำนวนรูปภาพ:
1. เพิ่มรูปใน `public/photos/`
2. แก้ไข array `photos` ใน `app/components/PhotoGallery.tsx`

---

## ❗ Troubleshooting

### รูปไม่แสดง:
- ตรวจสอบว่าชื่อไฟล์ตรงกับที่กำหนด (1.jpg - 10.jpg)
- ตรวจสอบว่าไฟล์อยู่ในโฟลเดอร์ `public/photos/` ถูกต้อง

### เพลงไม่เล่น:
- ตรวจสอบว่าไฟล์ชื่อ `birthday-song.mp3` อยู่ใน `public/music/`
- บาง browser อาจบล็อก autoplay - ให้กดปุ่ม Play Music เอง

### Build error:
```bash
npm install
npm run build
```

---

## 📱 Browser Support

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

## 💝 สนุกกับเว็บไซต์วันเกิด!

หวังว่าแฟนของคุณจะชอบและมีความสุขมากๆ นะคะ!
